/**
 * This component is responsible for holding
 * information for querying the renderer
 */
const Coordinates = {
    offset: 0,
    limit: 10,
    total: 151,
    currentPokemon: null,
    listView: true
}

export default Coordinates;